import { AppBar, Toolbar } from '@mui/material'

// const StyledN=styled(AppBar)`
// background:red;
// `

const NavBar = () => {
  return (
    <AppBar>
      <Toolbar></Toolbar>
    </AppBar>
  )
}

export default NavBar
